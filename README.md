# Slow but FastAPI
* FastAPI 실습용 프로젝트
* 배우느라 느리지만 API는 빨라요
* AI 없이 직접 만듭니다..

## How to Build
```bash
python3 -m venv .venv
source .venv/bin/activate
```
### FastAPI 설치
```bash
pip install fastapi
pip install "fastapi[all]"
```
